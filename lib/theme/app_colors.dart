import 'package:flutter/material.dart';

/// Paleta baseada no design "Constancia - telas do app" (Figma).
class AppColors {
  AppColors._();

  static const primary = Color(0xFF2E5C50); // verde escuro (botões/destaques)
  static const primaryDark = Color(0xFF1F4038);
  static const background = Color(0xFFEEECE6); // off-white de fundo
  static const surface = Color(0xFFFFFFFF);
  static const accentOrange = Color(0xFFE2793D); // ciclo de foco / streak
  static const statusParada = Color(0xFFD9534F); // card parado
  static const statusDesenvolvimento = Color(0xFF4C6FA0); // card em andamento
  static const statusNaoIniciada = Color(0xFF9B9B93); // card não iniciado
  static const textPrimary = Color(0xFF1E211F);
  static const textSecondary = Color(0xFF6B6E68);
  static const divider = Color(0xFFDAD8D0);
}
